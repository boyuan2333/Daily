# UI-005 Route List Information Hierarchy Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make Planning > Routes present real lifecycle groups and useful paused-route return context without changing execution state.

**Architecture:** Add a pure presentation helper over the existing `AppState`, `Route`, and `ExecutionSnapshot` records. The WinUI route list will render those sections and use explicit disclosure controls for read-only paused context. Project grouping remains visibly unavailable because the domain model has no project field; no fake project values will be introduced.

**Tech Stack:** .NET 10, WinUI 3, C#, xUnit.

---

### Task 1: Define route-list presentation behavior

**Files:**
- Create: `src/ExecutionContinuity.App/RouteListPresentation.cs`
- Test: `tests/ExecutionContinuity.App.Tests/ExecutionSessionTests.cs`

1. Add failing tests for status section ordering, paused-route ordering by newest valid snapshot, progress, and snapshot-backed paused context.
2. Run the focused tests and confirm they fail because the presentation API is missing.
3. Add the smallest pure helper and records needed by those tests.
4. Run the focused tests again.

### Task 2: Render grouped Routes UI

**Files:**
- Modify: `src/ExecutionContinuity.App/MainWindow.xaml`
- Modify: `src/ExecutionContinuity.App/MainWindow.xaml.cs`
- Test: `tests/ExecutionContinuity.App.Tests/ExecutionSessionTests.cs`

1. Add a stable grouping control with status selected by default and project visibly disabled with an explanation.
2. Replace the flat route loop with grouped sections and read-only route rows.
3. Add paused-route disclosure showing completion standard, boundary, note, and fallback.
4. Keep selection, disclosure, and grouping presentation-only; route activation remains an explicit command.
5. Run focused tests and build.

### Task 3: Document the boundary and verify

**Files:**
- Modify: `docs/UI_FIX_BACKLOG.md`
- Modify: `docs/MANUAL_ACCEPTANCE.md`

1. Record the completed status-grouping work and the project-model blocker.
2. Add manual checks for grouped status rows and paused disclosure.
3. Run the full build, test suite, and `git diff --check`.
4. Review the final diff and remind the user to decide whether to merge.
